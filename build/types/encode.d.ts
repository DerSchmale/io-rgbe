import { HDRImageData } from "./HDRImageData";
/**
 * Encodes the HDRImageData or ImageData (LDR) to the RGBE file format.
 */
export declare function encodeRGBE(imageData: HDRImageData | ImageData): ArrayBuffer;
